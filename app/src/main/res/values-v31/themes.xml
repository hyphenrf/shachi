<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.Shachi" parent="Theme.Material3.DynamicColors.Light">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Optional, if your app is drawing behind the status bar also. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:enforceNavigationBarContrast" tools:targetApi="q">false</item>
        <item name="android:enforceStatusBarContrast" tools:targetApi="q">false</item>
        <item name="android:windowLightStatusBar" tools:targetApi="q">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="q">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="p">shortEdges</item>
        <item name="postSlideToolbarBackground">@color/postSlideToolbarBackgroundColorLight</item>
    </style>
</resources>